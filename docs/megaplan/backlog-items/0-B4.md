<!-- megaplan v2.0.0 -->
# 0-B4 — Migration `101_drop_ecommerce.sql` + remover `priceCalculator`/queries produto/tipos 3D

| Field | Value |
|-------|-------|
| Status | done |
| Workflow step | COMPLETE |
| Owner | — |
| Verification | automated |
| Depends on | 0-B3 |
| Target | Cycle 0 |
| Last updated | 2026-07-28 |

## Outcome
O banco e o código deixam de ter a superfície de e-commerce: tabelas dropadas e
utilitários/queries/tipos de produto removidos.

## Scope
- [x] `docs/database/migrations/101_drop_ecommerce.sql` — drop `products`, `product_images`,
      `product_colors`, `product_sizes`, `colors`, `categories`, `orders`, `order_items`,
      `order_status_history`, `product_views` + views de relatório (031) + enum `order_status`
- [x] Remover `lib/utils/priceCalculator.ts`
- [x] Remover `lib/supabase/queries.ts` e `clientQueries.ts` (queries de produto/frete)
- [x] Remover `lib/validations/product.ts`
- [x] Remover campos 3D em `src/types/index.ts`
- [x] Aplicada contra o banco Supabase de produção (nefktxfcxcuglvgmrmfy) — confirmado

## Non-goals
- Remover `custom_orders` (fica; é orçamento)

## Dependencies / blockers
- 0-B3

## Test plan
| Level | File | Intent |
|-------|------|--------|
| Grep | — | `grep -rn "from('products')\|priceCalculator" src` → vazio |

## Acceptance criteria
- [x] `tsc`/testes/build verdes; status synced
- [x] Migration aplicada; `information_schema.tables` confirma só tabelas de tatuagem

## Traceability
- Related: Errata 2026-07-28

## Notes
Bug real encontrado e corrigido durante a aplicação: `gen_order_code()` (033) e o trigger
`trg_custom_orders_status_history` (027) dependiam em runtime de `orders`/`order_status_history`
— seriam quebrados pelo drop. Ambos foram redefinidos/removidos dentro da própria 101 antes do
`DROP TABLE`. Drop irreversível — já executado; dados de produto/pedido não existiam de verdade
(só seed placeholder).
