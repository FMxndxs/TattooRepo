<!-- megaplan v2.0.0 -->
# 0-B5 — Reduzir admin "Pedidos" → "Orçamentos" (`custom_orders`)

| Field | Value |
|-------|-------|
| Status | done |
| Workflow step | COMPLETE |
| Owner | — |
| Verification | automated |
| Depends on | 0-B4 |
| Target | Cycle 0 |
| Last updated | 2026-07-28 |

## Outcome
O admin mostra só orçamentos (`custom_orders`) com uma state machine própria, sem misturar
com pedidos de e-commerce.

## Scope
- [x] Simplificar `normalizeOrders` para só `custom_orders` (sem `orders`/`OrderType`)
- [x] Ajustar `AdminOrderRow`/`OrdersPanel` (sem tabs normal/custom, sem frete/itens/endereço/
      rastreio/etiqueta de produção)
- [x] State machine (`stateMachine.ts`/`service.ts`): pending → reviewing → quoted →
      accepted/rejected, com `cancelled` a partir de pending/reviewing
- [x] `docs/database/migrations/101_drop_ecommerce.sql` aperta `custom_orders_status_check`
      ao mesmo ciclo

## Non-goals
- Alterar fluxo de agendamento

## Dependencies / blockers
- 0-B4

## Test plan
| Level | File | Intent |
|-------|------|--------|
| Unit | `stateMachine.test.ts`, `orderService.test.ts`, `adminOrdersNormalize.test.ts`, `OrdersPanel.test.tsx`, `OrderStatusSelect.test.tsx` | transições e UI do ciclo de orçamento |

## Acceptance criteria
- [x] `tsc`/testes verdes; status synced

## Traceability
- Glossary: [[Orçamento / Custom order]]

## Notes
Removido também o botão "Confirmar WA" do OrdersPanel (chamava status `confirmed`, que não
existe no novo ciclo) — o link genérico "WhatsApp →" já cobre contato ad-hoc. Botão
equivalente para o novo ciclo (se desejado) fica para trabalho futuro, não faz parte deste
item.
